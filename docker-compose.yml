services:
  nienna-backend:
    build:
      context: .
    container_name: nienna-backend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8015:8015"
    volumes:
      - ./data:/srv/app/data
